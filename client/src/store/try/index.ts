import { createSlice } from "@reduxjs/toolkit";


const initialState={
    data:"sa"
}
const trySlice=createSlice({
    name:"selam",
    initialState,
    reducers:{}
})
export default trySlice.reducer